/*7)Crie uma função que receba uma string como parâmetro (de tamanho desconhecido) e retorne uma cópia da mesma. 
A assinatura da função deve ser:
char *strcopy(char *str);
*/

#include<stdio.h>

char *strcopy (char *str)
{
  char copia;
  *str = copia;
  
  return (copia);

}
